---
phase: 03-frontend-visualization
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - scripts/prepare-geo.ts
  - scripts/check-initial-bundle.mjs
  - public/data/geo/countries-110m.json
  - public/data/entity-crossref.json
  - public/data/latest.json
  - public/data/rankings.json
  - public/data/history.json
  - package.json
autonomous: true
requirements: [FE-01, FE-08, FE-09]
must_haves:
  truths:
    - "D-16/D-19: Country geography is loaded as a static public asset instead of being embedded in the JavaScript bundle"
    - "D-03/D-04: Public entity cross-reference data is materialized for browser drill-down and cluster proxy derivation"
    - "D-10/D-18: Aggregate latest, rankings, and history JSON exist before UI tests depend on them"
    - "D-16: Initial bundle measurement can fail the build when the entry chunk exceeds 300KB gzip"
  artifacts:
    - path: "scripts/prepare-geo.ts"
      provides: "Geo and browser-safe crossref materialization script"
    - path: "scripts/check-initial-bundle.mjs"
      provides: "FE-08 initial gzip bundle gate"
    - path: "public/data/geo/countries-110m.json"
      provides: "Static country TopoJSON asset"
    - path: "public/data/entity-crossref.json"
      provides: "Browser-safe hierarchy mapping used for drill-down and cluster proxy derivation"
    - path: "public/data/latest.json"
      provides: "Pipeline aggregate latest index"
    - path: "public/data/rankings.json"
      provides: "Pipeline aggregate rankings"
    - path: "public/data/history.json"
      provides: "Pipeline aggregate history"
  key_links:
    - from: "scripts/prepare-geo.ts"
      to: "public/data/geo/countries-110m.json"
      via: "copy from world-atlas package"
      pattern: "countries-110m\.json"
    - from: "scripts/prepare-geo.ts"
      to: "public/data/entity-crossref.json"
      via: "copy scripts/mappings/entity-crossref.json"
      pattern: "entity-crossref\.json"
    - from: "npm run pipeline"
      to: "public/data/latest.json, public/data/rankings.json, public/data/history.json"
      via: "Phase 2 compiler aggregate output"
      pattern: "latest\.json|rankings\.json|history\.json"
---

<objective>
Materialize the public static assets and bundle gate needed by the frontend: country boundaries, browser-safe crossref, aggregate JSON, and a 300KB gzip initial bundle check.

Purpose: FE-01/FE-08/FE-09 depend on public assets existing outside the JS bundle before map, ranking, detail, and trend plans run.
Output: Public geography/crossref/aggregate JSON files and repeatable asset/bundle scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-visualization/03-CONTEXT.md
@.planning/phases/03-frontend-visualization/03-RESEARCH.md
@.planning/phases/03-frontend-visualization/03-VALIDATION.md
@.planning/phases/03-frontend-visualization/03-01-SUMMARY.md
@.planning/phases/03-frontend-visualization/03-02-SUMMARY.md
@package.json
@scripts/compiler.ts
@scripts/mappings/entity-crossref.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize geography and public crossref assets</name>
  <files>scripts/prepare-geo.ts, public/data/geo/countries-110m.json, public/data/entity-crossref.json, package.json</files>
  <read_first>
    <file>package.json</file>
    <file>scripts/mappings/entity-crossref.json</file>
    <file>.planning/phases/03-frontend-visualization/03-RESEARCH.md</file>
  </read_first>
  <action>Create `scripts/prepare-geo.ts` that uses Node `fs/promises` and `createRequire(import.meta.url).resolve('world-atlas/countries-110m.json')` to copy the packaged TopoJSON file into `public/data/geo/countries-110m.json`, creating `public/data/geo/` when absent. In the same script, copy `scripts/mappings/entity-crossref.json` to `public/data/entity-crossref.json` so browser queries can fetch hierarchy data as a public static asset. Add `prepare:geo` package script as `tsx scripts/prepare-geo.ts` and run it so both public assets exist. This preserves the resolved public entity-crossref materialization blocker and enables downstream cluster proxy derivation from cloud-region crossref data.</action>
  <verify><automated>npm run prepare:geo && test -s public/data/geo/countries-110m.json && test -s public/data/entity-crossref.json</automated></verify>
  <acceptance_criteria>
    - `test -s public/data/geo/countries-110m.json` exits `0`
    - `test -s public/data/entity-crossref.json` exits `0`
    - `grep -v '^#' scripts/prepare-geo.ts | grep -c 'world-atlas/countries-110m.json'` returns `1`
    - `grep -v '^#' scripts/prepare-geo.ts | grep -c 'entity-crossref.json'` returns at least `1`
    - `grep -v '^#' package.json | grep -c '"prepare:geo": "tsx scripts/prepare-geo.ts"'` returns `1`
  </acceptance_criteria>
  <done>Country geography and browser-safe crossref are available as public static assets.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize aggregate JSON and add bundle gate</name>
  <files>scripts/check-initial-bundle.mjs, public/data/latest.json, public/data/rankings.json, public/data/history.json, package.json</files>
  <read_first>
    <file>package.json</file>
    <file>scripts/compiler.ts</file>
    <file>.planning/phases/03-frontend-visualization/03-RESEARCH.md</file>
  </read_first>
  <action>Create `scripts/check-initial-bundle.mjs` that reads `dist/assets`, identifies JavaScript files whose filename starts with `index-` and ends with `.js`, gzips each with `node:zlib`, and exits non-zero with text `Initial bundle exceeds 300KB gzip` if any entry chunk exceeds `300 * 1024` bytes. Add `bundle:check` script as `node scripts/check-initial-bundle.mjs`. Run `npm run pipeline` and verify `public/data/latest.json`, `public/data/rankings.json`, and `public/data/history.json` exist and are non-empty; if pipeline output is absent during execution, create deterministic fixture fallback files matching compiler aggregate contracts and document the fallback in the plan summary. Run `npm run build && npm run bundle:check`. This preserves the resolved valid error expression and the under-300KB initial bundle target.</action>
  <verify><automated>npm run pipeline && test -s public/data/latest.json && test -s public/data/rankings.json && test -s public/data/history.json && npm run build && npm run bundle:check</automated></verify>
  <acceptance_criteria>
    - `test -s public/data/latest.json` exits `0`
    - `test -s public/data/rankings.json` exits `0`
    - `test -s public/data/history.json` exits `0`
    - `grep -v '^#' scripts/check-initial-bundle.mjs | grep -c '300 \* 1024'` returns at least `1`
    - `grep -v '^#' scripts/check-initial-bundle.mjs | grep -c 'Initial bundle exceeds 300KB gzip'` returns at least `1`
    - `grep -v '^#' package.json | grep -c '"bundle:check": "node scripts/check-initial-bundle.mjs"'` returns `1`
  </acceptance_criteria>
  <done>Aggregate JSON exists for frontend consumers and the repository enforces the 300KB gzip initial bundle gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local pipeline/scripts -> public static files | Generated and copied JSON/TopoJSON becomes browser-consumed public data. |
| Build output -> public users | Oversized initial bundles can deny usable startup on constrained networks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | `scripts/prepare-geo.ts` | mitigate | Copy known package asset and checked-in crossref mapping to deterministic public paths. |
| T-03-08 | Denial of Service | aggregate JSON availability | mitigate | Verify latest/rankings/history are non-empty before downstream UI plans depend on them. |
| T-03-09 | Denial of Service | `scripts/check-initial-bundle.mjs` | mitigate | Fail `npm run bundle:check` when the initial JS entry chunk exceeds 300KB gzip. |
</threat_model>

<verification>
```bash
npm run prepare:geo
test -s public/data/geo/countries-110m.json
test -s public/data/entity-crossref.json
npm run pipeline
test -s public/data/latest.json
test -s public/data/rankings.json
test -s public/data/history.json
npm run build
npm run bundle:check
npm test
```
</verification>

<success_criteria>
- `public/data/geo/countries-110m.json` exists and is non-empty.
- `public/data/entity-crossref.json` exists and is non-empty.
- `public/data/latest.json`, `public/data/rankings.json`, and `public/data/history.json` exist and are non-empty.
- `npm run bundle:check` enforces the exact `300 * 1024` gzip limit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-visualization/03-03-SUMMARY.md`.
</output>
