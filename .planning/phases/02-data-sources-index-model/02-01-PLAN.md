---
phase: 02-data-sources-index-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run-pipeline.ts
  - scripts/types.ts
  - scripts/mappings/entity-crossref.json
  - scripts/mappings/export-control-tiers.json
  - public/data/index-config.json
autonomous: true
requirements: [PIPE-05, INDX-03, INDX-04, INDX-06, INDX-07]
must_haves:
  truths:
    - "Pipeline skips compile when all scrapers return empty and no existing entities (CR-01 fixed)"
    - "Entity files always sort series by timestamp ascending and latest points to most recent (CR-02 fixed)"
    - "entity-crossref.json maps all 10 MVP countries, 20 cities, AWS+Azure cloud regions, and 5 companies"
    - "export-control-tiers.json assigns correct tier multiplier for each MVP country"
    - "index-config.json defines 5 factors with weights summing to 1.0 and confidence scoring parameters"
    - "types.ts exports IndexConfig, ExportControlTiers, EntityCrossRef interfaces and extended CompiledEntity"
  artifacts:
    - path: "scripts/run-pipeline.ts"
      provides: "CR-01 empty compile guard, CR-02 timestamp sort, defensive empty-array guard"
      contains: "sortedByTime"
    - path: "scripts/types.ts"
      provides: "IndexConfig, ExportControlTiers, EntityCrossRef, extended CompiledEntity"
      exports: ["IndexConfig", "ExportControlTiers", "EntityCrossRef"]
    - path: "scripts/mappings/entity-crossref.json"
      provides: "Cross-source entity mapping for 10 countries, cloud regions, cities, companies"
      contains: "countries"
    - path: "scripts/mappings/export-control-tiers.json"
      provides: "Three export control tiers with multipliers and country assignments"
      contains: "multiplier"
    - path: "public/data/index-config.json"
      provides: "Factor weights and confidence parameters"
      contains: "factors"
  key_links:
    - from: "scripts/types.ts"
      to: "scripts/mappings/entity-crossref.json"
      via: "EntityCrossRef type describes the JSON schema"
      pattern: "interface EntityCrossRef"
    - from: "scripts/types.ts"
      to: "public/data/index-config.json"
      via: "IndexConfig type describes the JSON schema"
      pattern: "interface IndexConfig"
---

<objective>
Fix Phase 1 bugs (CR-01, CR-02), extend TypeScript types for Phase 2 index model, and create all static configuration files (entity cross-reference, export control tiers, index weights).

Purpose: Every subsequent Phase 2 plan depends on these types and config files. Fixing bugs now prevents building on broken foundations.
Output: Updated types.ts with new interfaces, fixed run-pipeline.ts, three JSON config files, one JSON mapping file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-sources-index-model/02-CONTEXT.md
@.planning/phases/02-data-sources-index-model/02-RESEARCH.md
@.planning/phases/02-data-sources-index-model/02-PATTERNS.md
@scripts/types.ts
@scripts/run-pipeline.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01/CR-02 bugs in run-pipeline.ts and extend types.ts</name>
  <files>scripts/run-pipeline.ts, scripts/types.ts</files>
  <read_first>
    - scripts/run-pipeline.ts (current pipeline orchestrator with CR-01/CR-02 bugs)
    - scripts/types.ts (current type definitions to extend)
    - .planning/phases/01-pipeline-skeleton/01-REVIEW.md (CR-01 and CR-02 fix specifications at lines 43-80)
    - .planning/phases/02-data-sources-index-model/02-PATTERNS.md (type extension pattern at lines 292-343)
  </read_first>
  <action>
**CR-02 fix in `writeEntityIfChanged` (scripts/run-pipeline.ts):**
Replace line 52 (`latest: records[records.length - 1],`) with timestamp-sorted selection:
```typescript
const sortedByTime = [...records].sort((a, b) => a.timestamp.localeCompare(b.timestamp));
const entityFile: EntityFile = {
  id: entityId,
  type: entityType,
  latest: sortedByTime[sortedByTime.length - 1],
  series: sortedByTime,
  _hash: newHash,
  _updatedAt: new Date().toISOString(),
};
```

**CR-01 fix in `run()` function (scripts/run-pipeline.ts):**
After the scraper loop (after line 87, before `await compile(DATA_DIR)` on line 90), add empty-compile guard:
```typescript
if (written === 0 && Object.keys(meta.entities).length === 0) {
  console.warn('[pipeline] No data produced and no existing entities. Skipping compile.');
  return;
}
```

**WR-03 fix:** Add defensive guard at line 81:
```typescript
for (const [entityId, entityRecords] of byEntity) {
  if (entityRecords.length === 0) continue;
  const entityType = entityRecords[0].entity.type;
```

**Extend CompiledEntity in scripts/types.ts:**
Add the following optional fields after `lastUpdated` (line 101):
```typescript
  /** Export control risk tier name */
  riskTier?: string;
  /** Export control risk multiplier (0.3-1.0) */
  riskMultiplier?: number;
  /** Per-factor breakdown: raw value, normalized 0-100, and weight */
  factorBreakdown?: Record<string, { raw: number; normalized: number; weight: number }>;
  /** Data completeness: 'full' if >=3 factors, 'partial' if <3 */
  dataCompleteness?: 'full' | 'partial';
```

**Add new types at end of scripts/types.ts:**

```typescript
/** Index configuration loaded from public/data/index-config.json */
export interface IndexConfig {
  version: number;
  factors: Record<string, { weight: number; description: string }>;
  confidence: {
    staleDays: number;
    stalePenalty: number;
    veryStaleDays: number;
    veryStalePenalty: number;
    missingFactorPenalty: number;
    minConfidence: number;
  };
}

/** Export control tier definitions loaded from scripts/mappings/export-control-tiers.json */
export interface ExportControlTiers {
  version: number;
  tiers: Record<string, { multiplier: number; description: string }>;
  countries: Record<string, string>;
}

/** Entity cross-reference mapping loaded from scripts/mappings/entity-crossref.json */
export interface EntityCrossRef {
  countries: Record<string, {
    iso2: string;
    iso3: string;
    worldBankCode: string;
    owidName: string;
    name: string;
  }>;
  cloudRegions: Record<string, { country: string; city: string; provider: string }>;
  cities: Record<string, { country: string; name: string }>;
  companies: Record<string, { cik: string; country: string; name: string }>;
}
```
  </action>
  <behavior>
    - CR-01: When pipeline runs with zero records and zero existing entities, compile() is NOT called (console.warn emitted instead)
    - CR-02: Entity file series is sorted by timestamp ascending; latest points to the last element after sort
    - WR-03: Empty entityRecords array is skipped (no TypeError)
    - types.ts exports IndexConfig, ExportControlTiers, EntityCrossRef interfaces
    - CompiledEntity has optional riskTier, riskMultiplier, factorBreakdown, dataCompleteness fields
  </behavior>
  <verify>
    <automated>cd /Users/yiwei/ics/iait && npx vitest run scripts/__tests__/orchestrator.test.ts scripts/__tests__/compiler.test.ts scripts/__tests__/types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run-pipeline.ts contains `const sortedByTime = [...records].sort((a, b) => a.timestamp.localeCompare(b.timestamp))`
    - scripts/run-pipeline.ts contains `if (written === 0 && Object.keys(meta.entities).length === 0)`
    - scripts/run-pipeline.ts contains `if (entityRecords.length === 0) continue;`
    - scripts/types.ts contains `interface IndexConfig`
    - scripts/types.ts contains `interface ExportControlTiers`
    - scripts/types.ts contains `interface EntityCrossRef`
    - scripts/types.ts contains `riskTier?: string`
    - scripts/types.ts contains `factorBreakdown?: Record<string, { raw: number; normalized: number; weight: number }>`
    - All existing tests pass: `npx vitest run` exits 0
    - `npx tsc --noEmit -p tsconfig.scripts.json` exits 0
  </acceptance_criteria>
  <done>CR-01 and CR-02 are fixed, WR-03 is fixed, types.ts exports IndexConfig/ExportControlTiers/EntityCrossRef interfaces, CompiledEntity has optional Phase 2 fields, all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create entity-crossref, export-control-tiers, and index-config JSON files</name>
  <files>scripts/mappings/entity-crossref.json, scripts/mappings/export-control-tiers.json, public/data/index-config.json</files>
  <read_first>
    - .planning/phases/02-data-sources-index-model/02-CONTEXT.md (D-04 entity crossref, D-06 MVP scope, D-19/D-20 export control tiers, D-02 index weights)
    - .planning/phases/02-data-sources-index-model/02-RESEARCH.md (section 3.5 crossref structure, section 3.4 risk tiers, section 6.1 index config)
    - .planning/phases/02-data-sources-index-model/02-PATTERNS.md (lines 368-403 for EntityCrossRef shape, lines 545-547 for JSON config pattern)
  </read_first>
  <action>
**Create `scripts/mappings/entity-crossref.json`** (per D-04, D-05, D-06):

MVP scope: 10 countries (us, cn, de, gb, jp, in, sg, nl, ie, ca), 5 cloud providers (AWS, Azure, GCP, Oracle, IBM), ~20 cities, 5 companies.

```json
{
  "countries": {
    "us": { "iso2": "US", "iso3": "USA", "worldBankCode": "USA", "owidName": "United States", "name": "United States" },
    "cn": { "iso2": "CN", "iso3": "CHN", "worldBankCode": "CHN", "owidName": "China", "name": "China" },
    "de": { "iso2": "DE", "iso3": "DEU", "worldBankCode": "DEU", "owidName": "Germany", "name": "Germany" },
    "gb": { "iso2": "GB", "iso3": "GBR", "worldBankCode": "GBR", "owidName": "United Kingdom", "name": "United Kingdom" },
    "jp": { "iso2": "JP", "iso3": "JPN", "worldBankCode": "JPN", "owidName": "Japan", "name": "Japan" },
    "in": { "iso2": "IN", "iso3": "IND", "worldBankCode": "IND", "owidName": "India", "name": "India" },
    "sg": { "iso2": "SG", "iso3": "SGP", "worldBankCode": "SGP", "owidName": "Singapore", "name": "Singapore" },
    "nl": { "iso2": "NL", "iso3": "NLD", "worldBankCode": "NLD", "owidName": "Netherlands", "name": "Netherlands" },
    "ie": { "iso2": "IE", "iso3": "IRL", "worldBankCode": "IRL", "owidName": "Ireland", "name": "Ireland" },
    "ca": { "iso2": "CA", "iso3": "CAN", "worldBankCode": "CAN", "owidName": "Canada", "name": "Canada" }
  },
  "cloudRegions": {
    "aws-us-east-1": { "country": "us", "city": "ashburn", "provider": "aws" },
    "aws-us-west-2": { "country": "us", "city": "portland", "provider": "aws" },
    "aws-eu-west-1": { "country": "ie", "city": "dublin", "provider": "aws" },
    "aws-eu-central-1": { "country": "de", "city": "frankfurt", "provider": "aws" },
    "aws-ap-northeast-1": { "country": "jp", "city": "tokyo", "provider": "aws" },
    "aws-ap-southeast-1": { "country": "sg", "city": "singapore", "provider": "aws" },
    "aws-sa-east-1": { "country": "br", "city": "sao-paulo", "provider": "aws" },
    "azure-eastus": { "country": "us", "city": "ashburn", "provider": "azure" },
    "azure-westus3": { "country": "us", "city": "portland", "provider": "azure" },
    "azure-westeurope": { "country": "nl", "city": "amsterdam", "provider": "azure" },
    "azure-northeurope": { "country": "ie", "city": "dublin", "provider": "azure" },
    "azure-japaneast": { "country": "jp", "city": "tokyo", "provider": "azure" },
    "azure-southeastasia": { "country": "sg", "city": "singapore", "provider": "azure" },
    "azure-centralindia": { "country": "in", "city": "pune", "provider": "azure" },
    "azure-canadacentral": { "country": "ca", "city": "toronto", "provider": "azure" },
    "azure-uksouth": { "country": "gb", "city": "london", "provider": "azure" },
    "azure-germanywestcentral": { "country": "de", "city": "frankfurt", "provider": "azure" },
    "azure-chinaeast2": { "country": "cn", "city": "shanghai", "provider": "azure" },
    "gcp-us-central1": { "country": "us", "city": "council-bluffs", "provider": "gcp" },
    "gcp-europe-west1": { "country": "be", "city": "st-ghislain", "provider": "gcp" },
    "gcp-asia-east1": { "country": "tw", "city": "changhua", "provider": "gcp" },
    "oracle-us-ashburn-1": { "country": "us", "city": "ashburn", "provider": "oracle" },
    "ibm-us-south": { "country": "us", "city": "dallas", "provider": "ibm" }
  },
  "cities": {
    "ashburn": { "country": "us", "name": "Ashburn, VA" },
    "portland": { "country": "us", "name": "Portland, OR" },
    "dublin": { "country": "ie", "name": "Dublin" },
    "frankfurt": { "country": "de", "name": "Frankfurt" },
    "tokyo": { "country": "jp", "name": "Tokyo" },
    "singapore": { "country": "sg", "name": "Singapore" },
    "sao-paulo": { "country": "br", "name": "Sao Paulo" },
    "amsterdam": { "country": "nl", "name": "Amsterdam" },
    "pune": { "country": "in", "name": "Pune" },
    "toronto": { "country": "ca", "name": "Toronto" },
    "london": { "country": "gb", "name": "London" },
    "shanghai": { "country": "cn", "name": "Shanghai" },
    "council-bluffs": { "country": "us", "name": "Council Bluffs, IA" },
    "st-ghislain": { "country": "be", "name": "St. Ghislain" },
    "changhua": { "country": "tw", "name": "Changhua County" },
    "dallas": { "country": "us", "name": "Dallas, TX" },
    "san-jose": { "country": "us", "name": "San Jose, CA" },
    "mumbai": { "country": "in", "name": "Mumbai" },
    "osaka": { "country": "jp", "name": "Osaka" },
    "sydney": { "country": "au", "name": "Sydney" }
  },
  "companies": {
    "nvidia": { "cik": "0001045810", "country": "us", "name": "NVIDIA" },
    "microsoft": { "cik": "0000789019", "country": "us", "name": "Microsoft" },
    "alphabet": { "cik": "0001652044", "country": "us", "name": "Alphabet/Google" },
    "amazon": { "cik": "0001018724", "country": "us", "name": "Amazon" },
    "meta": { "cik": "0001326801", "country": "us", "name": "Meta" }
  }
}
```

**Create `scripts/mappings/export-control-tiers.json`** (per D-19, D-20):

```json
{
  "version": 1,
  "tiers": {
    "unrestricted": { "multiplier": 1.0, "description": "No export restrictions on AI compute hardware" },
    "restricted": { "multiplier": 0.7, "description": "Partial restrictions on advanced AI chips" },
    "sanctioned": { "multiplier": 0.3, "description": "Broad restrictions on AI compute exports" }
  },
  "countries": {
    "us": "unrestricted",
    "gb": "unrestricted",
    "de": "unrestricted",
    "nl": "unrestricted",
    "ie": "unrestricted",
    "ca": "unrestricted",
    "jp": "unrestricted",
    "sg": "unrestricted",
    "in": "restricted",
    "cn": "sanctioned"
  }
}
```

**Create `public/data/index-config.json`** (per D-01, D-02, D-03, D-07, D-08, D-09):

Weights from D-02: GPU_Supply 0.30, Energy_Capacity 0.20, Cloud_Region_Density 0.15, AI_CapEx 0.25, Risk_Adjustment 0.10. Sum = 1.00.

```json
{
  "version": 1,
  "factors": {
    "gpu_supply": { "weight": 0.30, "description": "GPU pricing density across cloud regions (inverse price = higher supply)" },
    "energy_capacity": { "weight": 0.20, "description": "Power generation and consumption capacity" },
    "cloud_region_density": { "weight": 0.15, "description": "Number of cloud regions/zones per entity" },
    "ai_capex": { "weight": 0.25, "description": "Company AI capital expenditure (annualized TTM)" },
    "risk_adjustment": { "weight": 0.10, "description": "Export control risk tier multiplier" }
  },
  "confidence": {
    "staleDays": 30,
    "stalePenalty": 1,
    "veryStaleDays": 90,
    "veryStalePenalty": 2,
    "missingFactorPenalty": 1,
    "minConfidence": 1
  }
}
```

Ensure `scripts/mappings/` directory is created with `mkdir -p`. Ensure `public/data/` directory exists.
  </action>
  <verify>
    <automated>cd /Users/yiwei/ics/iait && node -e "const c = JSON.parse(require('fs').readFileSync('scripts/mappings/entity-crossref.json','utf8')); const e = JSON.parse(require('fs').readFileSync('scripts/mappings/export-control-tiers.json','utf8')); const i = JSON.parse(require('fs').readFileSync('public/data/index-config.json','utf8')); const wsum = Object.values(i.factors).reduce((s,f) => s + f.weight, 0); console.log('countries:', Object.keys(c.countries).length); console.log('cloudRegions:', Object.keys(c.cloudRegions).length); console.log('cities:', Object.keys(c.cities).length); console.log('companies:', Object.keys(c.companies).length); console.log('tiers:', Object.keys(e.tiers).length); console.log('country-tiers:', Object.keys(e.countries).length); console.log('weight-sum:', wsum); console.log('factors:', Object.keys(i.factors).length); console.log('confidence-params:', Object.keys(i.confidence).length);"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/mappings/entity-crossref.json is valid JSON with "countries", "cloudRegions", "cities", "companies" keys
    - entity-crossref.json countries has exactly 10 entries: us, cn, de, gb, jp, in, sg, nl, ie, ca
    - entity-crossref.json companies has exactly 5 entries with correct CIK numbers (nvidia: 0001045810, microsoft: 0000789019, alphabet: 0001652044, amazon: 0001018724, meta: 0001326801)
    - export-control-tiers.json has exactly 3 tiers: unrestricted (1.0), restricted (0.7), sanctioned (0.3)
    - export-control-tiers.json countries has cn mapped to "sanctioned", in mapped to "restricted"
    - index-config.json weight sum equals 1.0 (0.30 + 0.20 + 0.15 + 0.25 + 0.10)
    - index-config.json has 5 factors and 6 confidence parameters (staleDays, stalePenalty, veryStaleDays, veryStalePenalty, missingFactorPenalty, minConfidence)
    - index-config.json confidence.minConfidence equals 1
    - All three files have "version": 1
  </acceptance_criteria>
  <done>entity-crossref.json maps 10 countries, 20+ cities, 23+ cloud regions, 5 companies. export-control-tiers.json defines 3 tiers with correct multipliers and country assignments. index-config.json has 5 factors summing to 1.0 with confidence scoring parameters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config→compiler | index-config.json weights are user-configurable; malformed weights could produce invalid scores |
| mapping→index | entity-crossref.json maps between data source ID systems; incorrect mappings cause wrong entity associations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | index-config.json | mitigate | Validate weights sum to 1.0 at load time in compiler; reject config with non-finite or negative weights |
| T-02-02 | Tampering | entity-crossref.json | accept | Static file committed to repo; only developers can modify; no user input |
| T-02-03 | Information Disclosure | SEC_EDGAR_EMAIL env var | accept | Email only used in User-Agent header for SEC API compliance; not logged to output files |
</threat_model>

<verification>
After this plan completes:
1. `npx tsc --noEmit -p tsconfig.scripts.json` exits 0
2. `npx vitest run` exits 0 (all existing tests still pass)
3. scripts/run-pipeline.ts has CR-01/CR-02/WR-03 fixes
4. scripts/types.ts exports 3 new interfaces + 4 new CompiledEntity optional fields
5. Three config JSON files exist and are valid with correct values
</verification>

<success_criteria>
- CR-01 fixed: compile() guarded against empty data
- CR-02 fixed: entity files sort by timestamp, latest = most recent
- WR-03 fixed: defensive empty-array guard
- EntityCrossRef, ExportControlTiers, IndexConfig types exported from types.ts
- CompiledEntity extended with riskTier, riskMultiplier, factorBreakdown, dataCompleteness
- entity-crossref.json: 10 countries, 20+ cities, 23+ cloud regions, 5 companies
- export-control-tiers.json: 3 tiers, 10 country assignments
- index-config.json: 5 factors summing to 1.0, 6 confidence parameters
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-sources-index-model/02-01-SUMMARY.md`
</output>
